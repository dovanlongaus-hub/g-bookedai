import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Get Started — AI Mentoring from $29 | Longcare AU',
  description: 'Book your first AI mentoring session from $29 AUD. 1-on-1 Google Meet sessions with AI-generated notes. Start today.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
